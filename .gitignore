# Google service account credentials (never commit these)
eng-lightning-496121-k0-486b539c0d3e.json
creds.json

# Logs
*.log

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# Output data
output/
*.csv

# Anonymous demand store and its HMAC key. The key MUST never be committed —
# with it, the contact digests become reversible against the ~10^10 US phone
# number space, which is the whole thing the digest exists to prevent.
hope702_activity.db
hope702_activity.db-*
.contact_secret
